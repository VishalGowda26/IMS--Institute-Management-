/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { LpaPipe } from './lpa.pipe';

describe('Pipe: Lpae', () => {
  it('create an instance', () => {
    let pipe = new LpaPipe();
    expect(pipe).toBeTruthy();
  });
});
